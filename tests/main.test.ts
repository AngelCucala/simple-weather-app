//Test trivial para verificar el funcionamiento de las librerías

import { it, expect, describe } from 'vitest'

describe('group', () => {

     it('should', () => {
        expect(1).toBeTruthy();
    })
})